# Contribuindo

Use Node.js 20 ou superior, execute `npm install` e rode `npm run check` antes de abrir um pull request. Toda extensão deve incluir entradas de frontend e backend, manifesto validado, capacidades declaradas, testes e documentação. Preserve compatibilidade das APIs e nunca versione credenciais ou artefatos gerados.
