# Docker

The Docker extension provides a full-stack container view: its frontend renders Docker resources in the Workbench and its backend mediates image and container operations through declared workspace and process capabilities.
