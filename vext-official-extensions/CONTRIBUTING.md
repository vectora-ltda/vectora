# Contributing

Install Node.js 20 or newer, run `npm install`, and use `npm run check` before opening a pull request. New extensions must include frontend and backend entrypoints, a validated manifest, capability declarations, tests, and documentation. Keep public APIs backwards compatible and never commit credentials or generated build output.
