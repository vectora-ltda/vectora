# Contribuir

Usa Node.js 20 o posterior, ejecuta `npm install` y corre `npm run check` antes de abrir un pull request. Cada extensión debe incluir entradas de frontend y backend, manifiesto validado, capacidades declaradas, pruebas y documentación. Conserva la compatibilidad de las APIs y no subas credenciales ni artefactos generados.
